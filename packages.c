#include<stdio.h>

void swap(int* x, int* y)
{
    int temp;
    temp = *x;
    *x = *y;
    *y = temp;
}

int* random_arr(int low, int up, int length)
{
    // 生成范围在[low, up]之间的，长度为 length 的随机数
    int* arr;

    return arr;
}
